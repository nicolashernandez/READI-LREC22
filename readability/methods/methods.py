"""
The methods module acts as an interface for Machine Learning applications (SVM/MLP and so on)

It should be able to provide the following :
Create the applications
Customize/Tune the applications
Using the applications
Export the results | Output visualisations
"""

#Checklist :
#Ignore this for now until the deep learning stuff is all ready
#Just allow to reproduce the paper's contents since a true library isn't what we want.

#from sklearn.linear_model import LogisticRegression
#from sklearn.ensemble import RandomForestClassifier
#from sklearn.svm import LinearSVC
#from sklearn.model_selection import cross_val_score
#from sklearn.model_selection import train_test_split
#from sklearn.feature_extraction.text import CountVectorizer
#from sklearn.feature_extraction.text import TfidfTransformer
#from sklearn.naive_bayes import MultinomialNB
#from sklearn.neural_network import MLPClassifier


#dummy tokenizer for tf idf method
#def dummy_fun(doc):
#    return doc


#levels = None
#tokens_split = None

#tokens_split_list=list()
#labels_split = list()
#for level in levels:
#    for text in tokens_split[level]:
#      tex = []
#      labels_split.append(int(level[-1]))
#      for sent in text:
#        for token in sent:
#          tex.append(token.replace('\u200b',''))
#      tokens_split_list.append(tex)
#It converts a text (list(list())) into just a list()? Need to check.

#Configuration :




#Using a specific model



#Using multiple model



#Using the same models as in the article




